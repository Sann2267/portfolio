-r requirements.txt

# ---- tests and quality ---------------------------------------------------------
pytest>=8.3.0
ruff>=0.6.0

# ---- content extraction tooling (source PDFs in docs/sources/, never at runtime)
pdfplumber>=0.11.0
